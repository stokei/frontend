query GetCurrentAppFinancial {
  currentApp {
    id
    balances {
      currency {
        id
        symbol
        minorUnit
      }
      pendingAmount
      availableAmount
      paymentGatewayType
    }
  }
}
