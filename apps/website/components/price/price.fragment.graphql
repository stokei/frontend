fragment PriceComponent on Price {
  id
  parent
  nickname
  fromAmount
  amount
  discountPercent
  unit
  active
  isDefault
  currency {
    id
    symbol
    minorUnit
  }
  billingScheme
  tiersMode
  tiers {
    items {
      id
      amount
      upTo
      infinite
    }
  }
  recurring {
    usageType
    intervalCount
    interval
  }
}
