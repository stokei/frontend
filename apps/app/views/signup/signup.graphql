mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    accessToken
    prefixToken
    refreshToken
    account {
      id
      fullname
      isAdmin
      app {
        id
        name
      }
      email
    }
  }
}
