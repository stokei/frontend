mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    accessToken
    prefixToken
    refreshToken
    account {
      id
      fullname
      app {
        id
        name
      }
      email
    }
  }
}
