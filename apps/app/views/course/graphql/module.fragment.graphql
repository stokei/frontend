fragment CoursePageModule on Module {
  id
  name
  videos(orderBy: { name: ASC }) {
    totalCount
    items {
      id
      name
      private
      active
      file {
        url
        duration
      }
    }
  }
}
