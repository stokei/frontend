query CurrentGlobalApp {
  currentApp {
    id
    name
    isStokei
    isIntegratedWithPix
    isIntegratedWithStripe
    stripeAccount
    currency {
      id
      symbol
      minorUnit
    }
    icon {
      file {
        url
      }
    }
    logo {
      file {
        url
      }
    }
    colors {
      items {
        color
        themeMode
        type
      }
    }
  }
}
