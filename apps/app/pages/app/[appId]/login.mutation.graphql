mutation Login($input: LoginInput!) {
  login(input: $input) {
    accessToken
    refreshToken
    prefixToken
    account {
      id
      fullname
      app {
        id
        name
      }
      email
    }
  }
}
