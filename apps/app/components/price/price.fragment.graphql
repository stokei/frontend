fragment PriceComponent on Price {
  id
  nickname
  fromAmount
  amount
  discountPercent
  unit
  active
  currency {
    id
    symbol
    minorUnit
  }
  billingScheme
  tiersMode
  tiers {
    items {
      id
      amount
      upTo
      infinite
    }
  }
  recurring {
    usageType
    intervalCount
    interval
  }
}
